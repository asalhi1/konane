/* Simple Konane entry point.
   Calls the UI main menu which drives the rest of the program. */
#include "game.h"
#include "ui.h"

int main(void) {
  /* Start the user interface / game menus */
  main_menu();
  
  return 0;
} 
